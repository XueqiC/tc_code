# 昨晚(9/6 01:19Z 方案采纳)到现在的全部结果汇总 — 2026-09-06 11:20 EDT

范围:采纳"条件对照证据"方案之后的全部工作。单种子;同机(hpg)训练评测;新付费 teacher 调用 0(四个凭据整晚 429/403)。

## 0. 一句话

- 工具链全部建好并通过测试(对构造、混淆探测、pair_unit 联合目标、三臂 runner、留出对评测、官方评测完整性检查)。
- **核心实证发现**:在历史池能配的 263 个状态 / 20,991 个候选对里,学生**没有参数级的"条件混淆"**(严格定义下绑定型只有 6 对,κ 在所有绑定对上为负);它的条件依赖失效只有一件事——**该不该调用**(生成的物理题上 191/204 状态用文字解题不调用;live/irrelevance 上 49/60 对过度调用)。
- 三臂 A/B/C(46 训练对、93 锚点、35 步、KL≈0.005)**训练完成且曝光匹配**,官方评测因 multi_turn_long_context 生成不完整失败一次,已修(完整性检查 + 重试)并在重评(hpg 41241770);留出对评测在排队。**还没有任何一个臂的分数。**

## 1. 时间线

| 时间(EDT) | 事件 |
|---|---|
| 21:19 | 采纳方案 + 我的补充(混淆的可操作定义 κ、泄漏隔离、调用率指标);Codex C19(对构造/探测)、C20(pair_unit 目标 + 三臂)启动 |
| 23:00–01:30 | C19/C20 交付;教师文本格式修复(37/39 过 checker);rg 缺失修复 |
| 01:50–04:10 | 探测 slurm 根目录/PATH 修复;首轮探测(213 对):严格混淆 9 对 |
| 04:10–05:30 | 发现生成池 id 大量复用(303 行 27 个 id)→ 按 prompt hash 保护,找回 simple_python_31 210 变体 → 候选 20,991 对 |
| 05:30–06:40 | 全量探测(yd24f,69 min):绑定型混淆 6、边界型 49;simple_python_31 零混淆(学生不调用) |
| 06:40–08:30 | 取消 1/3 上限(等你反对)、只留整个函数的划分:train 46 / heldout 9;三臂投 hpg |
| 08:30–10:30 | 三次启动前失败(空 y⁺、打乱无解、loader 契约)各修一次,均未耗 GPU |
| 10:40–12:00 | 三臂训练完成(73 min);A 官方评测失败(multi_turn_long_context 26/200);留出评测启动前失败(陈旧性检查) |
| 12:00–16:20 | C21b/C22 修好;隧道断 4 小时 |
| 17:10 | 隧道恢复;官方 eval-only(41241770 运行中)+ 留出对评测(41241772/3 排队)投出 |

## 2. 数据与探测结果

| 项 | 数 |
|---|---|
| 候选对(prompt-hash 泄漏规则后) | 20,991(绑定 20,874;调用/弃答 117;多轮 0,候选状态 24 待教师) |
| 唯一状态 | 263(4 + 1 个种子函数;simple_python_31 204) |
| 教师有效对 | 186 → 全量后 186+(37/39 文本经类型渲染通过) |
| 学生贪心至少一侧错 | 绑定 125/126(首轮),simple_python_31 191/204 状态 |
| **严格混淆对**(错 且 绑定更近另一侧 或 κ>0) | **绑定 6,调用/弃答 49,合计 55** |
| κ>0(偏好另一侧动作) | 绑定 0/41k 侧;调用/弃答 49/60 对 |
| 划分(取消上限、整函数留出 live_multiple_923) | train 46(边界 40 + 绑定 6)/ heldout 9 |

失效模式(base 学生):simple_python_31 → `wrong_count`(文字解题不调用);simple_javascript_42 → JS 参数类型 / 多塞 null;live_multiple_923 → 取值错;live_parallel_multiple_10 → 调用数错、JSON 坏。

## 3. 三臂训练(hpg yd24f 41233505)

| 臂 | 目标 | 步数 | 末 loss | KL 到 base | 状态 |
|---|---|---|---|---|---|
| A cc_s1_A_s0 | 普通 base-centred pairwise(展平两侧) | 35 | — | — | 训完,官方评测重跑中 |
| B cc_s1_B_s0 | 联合 worst-side margin,正确配对 | 35 | — | — | 训完,待评 |
| C cc_s1_C_s0 | 联合 worst-side margin,打乱配对(绑定 4 同函数重配、边界 24) | 35 | 0.106 | 0.0046 | 训完,待评 |
manifests 校验:三臂 token / 侧 / 锚点曝光 / 步数 / 机器一致。

## 4. 未出的结果与下一步

- 官方 v4 全量分(NL/Live/MT/Memory/Irrel + 整体):41241770(dept,240G,含生成完整性检查与重试)运行中,约 2–3 h 三臂。
- 留出对评测(未见函数 9 对两侧同时正确率、κ 翻转、修复/损伤、调用率 vs base):41241772/41241773 排队。
- 判读按方案:A 改善 → 证据有教学价值;B > A → 联合训练有额外收益;B > C → 条件关系参与学习;只有调用率变 → 没学到条件决策。
- 类型 2(多轮状态变化)等教师额度;AppWorld 暂停。

## 5. 修的坑(都是启动前失败,GPU 时 0)
教师文本类型渲染;rg 缺失;slurm 根目录 / ninja PATH;id 复用泄漏规则;弃答空文本;打乱无解;evaluate 陈旧性检查;multi_turn_long_context 生成不完整(campaign 加完整性检查 + 重试);隧道断 4 h。
