# 失败分析:两种子交叉矩阵(2026-08-14 下午)

数据源:mx_2b_seed0/1 + fam_b0/b1_rai 的 metrics.json(selection/train/eval 全量统计)。

## 1. "ours" 策展列为什么反而低(主要失败)

对比同一批采购下 plain vs ours 策展(mx 两种子):

| 格 | 行数 | 训练 token | 语料对 support 相似度 | 优化步 | exec |
|---|---|---|---|---|---|
| M_ours_plain (s0) | 168 | 19995 | .816 | 63 | **.933** |
| M_ours_ours (s0) | 136 | 18550 | .855 | 51 | .767 |
| M_ours_plain (s1) | 170 | 19992 | .809 | 66 | .833 |
| M_ours_ours (s1) | 132 | 18068 | .857 | 51 | .767 |

**机制诊断**:矩阵协议里策展发生在预算花完之后,门筛掉的 ~20% 行(7-9% token)是
**沉没成本,无法回购**;且被留下的语料对 support 的嵌入相似度系统性升高
(.81→.86)——门把多样性尾部剪掉了,语料更同质。两件事叠加:数据更少 + 更同质
→ in-task 下降。

**为什么综合臂(F_atom_boot3)不吃这个亏**:综合臂的门在**采购循环内**起作用
——拒收的货触发富余阶段回购,预算最终花满(无停版 .957±.02)。矩阵把门从采购
环节错位移植到策展环节,才产生损失。这与之前的实现级发现 #2(免费货架紧门选优、
付费采购宽门验货)同构:**门的正确位置取决于是否还有重新花钱的机会**。

**修复(今晚验证)**:策展列的 ours 改为"仅需求加权"(E3_CURATE_GATE=0),门保留
在采购臂内。重跑 M_selfinst_ours / M_ours_ours / M_ourscorr_ours × 2 seeds。

**保底口径**(若加权仍不加分,见 §3):策展段的主张收缩为"门的价值在边界纯度
(sqljoin 残留 0.000 vs .83-.87)与预算效率,不在 in-task 均值"。

## 2. 为什么 Self-Instruct 是最强 acquisition baseline

| 行 | 行数 | tok/行 | 预算花费 | exec (2 seeds) |
|---|---|---|---|---|
| selfinst | 181-184 | ~92 | 16.8k/20k (84%) | .883±.05 |
| evol | 70 | 147 | **10.3k/20k (51%)** | .667±.03 |
| llm2llm | 222-229 | ~89 | 20k (100%) | .817±.08 |
| ourscorr | 269-270 | ~74 | 20k (100%) | .867±.03 |
| ours | 168-170 | ~118 | 20k (100%) | .883±.05 |

- **Self-Instruct 的设计优势 = 广度优先 + 验证过滤 + 短样本**:多样种子指令、每行
  只 ~92 token,同预算下买到最多的**互异**on-task 样本;exec 检查保证正确性下限。
  在干净 few-shot 设定里,学生缺的正是覆盖面,广度直接变现。
  它的弱点不在 in-task,而在**无边界控制**(纯度任务上无引导采购的残留 .83-.87)
  和**无预算意识**(只花了 84%,也不知道该把 token 花在哪个技能上)。
- **Evol-Instruct 垫底的一半是我们的实现问题**:官方多轮进化,我们只跑了 1 轮
  ×2 分支,验证通过仅 70 变体 → **预算只花出去一半**;另一半是设计本身:难度
  进化使样本偏离 support 分布(147 tok/行,难度↑),对 2B 学生 + 小预算是
  distribution shift。**公平性修复已在跑**:按官方口径扩分支(--per-query 4),
  扩完后 evol 行整行 3 种子重跑(独立池文件,不污染现有表)。
- **LLM2LLM vs ourscorr(蒸馏侧对决的机制)**:两者唯一差异是增购导向。LLM2LLM
  对**错题实例**做变体 → 样本冗余(围着难例转)、方差大(.08);ourscorr 把失败
  过字典投影成**技能缺口方向**再买 → 每 token 买到更多互异样本(74 vs 89 tok/行,
  270 vs 226 行),均值高(.867 vs .817)且方差小(.03)。这正是"atom 词汇驱动
  学习"的直接证据格。

## 3. 蒸馏/策展侧横评(为什么 plain 这么强)

所有 acquisition 臂的产出都过了执行检查,语料正确性下限已经很高,策展的
"去噪"空间天然小:
- **AlpaGasus**:teacher 打分 94% 给 5.0(grade inflation,写进论文),过滤只
  砍掉明显残次 + 缩小数据量,效果在 plain 附近波动(.883±.05 vs .883±.05)。
  它设计上假设语料含大量低质(Alpaca 52k 场景),在"按需购买+验证"的设定下
  前提不成立。
- **LESS**:权重来自对 support 的梯度相似度;语料本来就是对着 support 买的,
  权重分布平,增益有限(.883±.02)。
- **我们的需求加权**:同理——在**已经 on-task 的语料**上,加权的边际价值低;
  它的判定场景是语料含边界外杂质时(sqljoin 门+权重把残留打到 0.000)。
  **结论候选**:策展段的贡献主张应绑定纯度/边界控制,而非 in-task 均值。

## 4. boot4(探针精化字典)为什么在 gsm 上无增益

boot4 = boot3 (.800=.800 seed1):k=50 的 support 对 gsm 覆盖已充分,LOO 方差挑出
的"低置信 atom"多是残差噪声原子,985 token 的探针买不来新信息。**boot4 的判定
实验应该是故意削弱 support(k-sweep / 偏斜子采样)**——支撑"少样本质量鲁棒性"
主张,列入排期,今晚不进 scope。

## 5. capability atom 设计本身的改进候选(记录,不进本次 scope)

1. **采购目标缺多样性项**:u(x)=Σmin(r,ŝ)/t̂ 只看技能供给,不看与已购样本的
   冗余 → 是 §1 同质化的源头之一。候选:u(x) 加边际增益/facility-location 项。
2. **字典规模拍脑袋**:n_components=min(64, k/2),可否由 reconstruction error
   的 elbow 或任务复杂度自适应。
3. **探针的目标函数**(boot4):当前只降"不确定性",可改为最大化 demand 估计的
   预期信息增益(主动学习口径,和 problem setting 的故事更一致)。

## 今晚执行序

1. seed2 双波收尾 → 3 种子主表(在跑)。
2. cur-fix 波:E3_CURATE_GATE=0,M_{selfinst,ours,ourscorr}_ours × seeds 0/1,
   用冻结 v1 池(data/evol_pool_v1_frozen.jsonl)保证与现有表同分布。
3. evol 扩池完成后:evol 行 3 种子公平重跑(v1+扩展池)。
4. 汇总 → tab:cross / tab:best / scope-freeze ⏳ 格。

## 晚间裁决(多种子,L4 波回传后)
- 去门修复三格同向 +.067~.100(✅);但需求加权 in-task 仍 ≤ plain。
- boot5/7 单 atom 探针验证判负:L1 份额可靠度 0.08-0.105,k=50/k=15 均无增益
  (桥仅聚合层面可用——u(x) 求和抵消误差;单 atom×单样本不可预测)。
- boot4 @k15×b20k 三种子 .833/.867/.700 vs boot3 .733/.867/.933:高方差,不稳定。
- boot6 均值噪声级(.778 vs .767),dw=0.25 单种子 .867 待复验;sqljoin 纯度不受损。
- evol 扩池(70→227,预算花满)后仍垫底 plain .689±.096:难度漂移是本质弱点。
- 战略观察:30 题评测集噪声 ±.09,in-task .75-.90 拥挤 → "稳定明显赢"应主张
  纯度/证书/预算效率/ourscorr,in-task 只求不输;明天扩评测集(→100)+ AppWorld。

## cfxsql 纯度判定(晚间,决定 v1 结构)
sqljoin 残留:boot3(环内门+回购)0.000;同一购单去门(M_ours_plain).900;
去门仅加权 .700;selfinst plain/加权 .933/1.000。
→ ① 纯度机制 = 采购环内 prompt 验货门(非配额定向);② 加权只在与字典同
词汇的语料上有部分贡献;③ v1 定型:采购=配额+环内门+回购+ourscorr,
训练=plain 等算力 SFT,部署=conformal 证书;其余组件降级为消融。

## AppWorld smoke 判定(深夜确认)
atoms/embedding/random/base(未训练)四臂在 dev40×12 步下部分得分全部 .276、
40/40 任务通过模式一致 → 该协议的部分得分是**空转地板**(不作为也过的
环境状态测试点),无方法区分度。修正方案(明日 B200):学生 4B/9B、
记分改"扣除 base 空转基线的净通过"、步数 12→24。

## H4 判定(8/15 上午,3 种子)
absorb 版 ourscorr(缺口来自训练动力学,免 rollout)外部 200 题:
.625/.610/.xxx,与 fail 版(.615/.650)同带——in-task 无增益,但等效前提下
省一整轮学生 rollout(~1/4 生成开销);滞后 atom 检出率 1/3 种子(gsm 语料
两 epoch 基本吸收完,触发面小)。定位:ourscorr 的效率变体 + AppWorld 上
更长训练/更难语料时滞后触发面大,是它的真判定场。

## AppWorld 记分协议终判(8/16 晨)
2B/4B × {12,24} 步 × {atoms,embedding,random,base} 全组合:部分得分
恒为 .276,且训练臂与未训练 base 的逐任务通过集合完全相同(0 个任务
有差异)。结论:dev40 的可通过测试点全部是"不作为也过"的环境状态
检查,该记分协议对策略完全不敏感。改造方向:①净通过口径(减 base
通过集)在此协议下必然全零,只能确证 null,不能出分;②真正出分需
更强学生(9B,B200 规模列在途)或全池/更大预算训练,或改用逐步
trace 相似度类指标。评测脚本已记 TODO:skip-if-done 防重评覆盖。

## 8/16 假设波总判定(外部 200 题)
- 统一化转正:字典边界 gdict(gsm .650 / sqljoin 残留 0.000)、统一
  准则 udens(.635 = 两阶段版)。松弛 s≤1.0 纯度不掉(残留 0.000),
  in-task 无增益 → 默认 s=0,旋钮进消融。
- 中性:R4 交替 .633=R2;统一停 20k 单种子 .590 偏低待复核。
- k-sweep 反转(诚实要点):boot3 k10/.595 k25/.613 k50/.635 平滑
  上升,SelfInst k10 即 .670 几乎不吃 k —— 少样本鲁棒性主张属于
  生成式基线;我们的小 k 短板与词汇恢复率(57%@k10)对应,由定向
  生成组件缓解(决胜格在途)。预算曲线 2k 档优势缩为 +.01(此前
  +.025 为部分种子)。
- 规模列:4B .823/.835、9B .897/.902 —— 并列随规模稳定。

## AppWorld aw4 波收官(8/16 晚)——首个非 null 信号,方向意外
4B@24 步:训练臂(atoms/embedding/random)部分得分均 .276,但
**未训练 base = .339**,且 base 与 atoms 逐任务通过集合有 7/40 差异
——首次出现策略敏感性,方向却是 **20k 演示 SFT 反而伤了 4B 的
AppWorld 部分得分**(候选解释:训练把输出格式拉向演示风格、破坏了
base 在环境检查上偶然可过的行为;或 20k 语料过窄致灾难性遗忘)。
这是可分析的真信号:下一步 ① 对比 base/训练臂在那 7 个分歧任务上的
轨迹找机制;② 全池(2506 行)重训验证"语料量不足"假设;③ 9B 列。

## 决胜格终判 + 环节级归因(8/17 下午)
gen2 三种子(v2 池,外部 200 题):boot3+定向生成 .680/.605/.600=.628,
selfinst 同池 .530/.605/.685=.607。均值 +.021,种子摆动 ±.08 → 统计平局,
"定向生成抬上限"未兑现。samp(自放大)重跑在途。

**跨轮环节归因(哪一环最值得改)**:本轮及之前所有"per-atom 定向"手段
全部中性或负——缺口纠错(≈)、curriculum(≈/负)、absorption-focus(负)、
定向生成(平)、boot5/7 探针(负);而同一套机器的**聚合层**用法全部有效
——u(x) 求和选取(赢基线)、环内门(纯度 0.000)、预算配额(浪费 2-4%)。
共同上游:**per-atom 信号信噪比**。桥的单 atom 可靠度实测 0.08-0.105,
吸收曲线的行归属是 argmax 主 atom(同样粗)。定向手段不是设计错了,
是方向信号本身被噪声淹没。

**可改进环节排名**:
1. II-2 桥(最大头寸):把"预测响应特征"换成"廉价测量"——先花极小预算
   请 teacher 对候选给一行解题草稿(~15-30 token),在草稿上算真实响应
   视角梯度特征再过字典。成本 ≈ 预算 3-5%,per-atom 信号从 0.1 换代。
   连带激活全部下游定向组件(纠错、生成、curriculum 可复审)。
2. III 新训练信号:既往失败共性 = 只重排/重权同一批 token。未试过加
   **新信号**:(a) selfamp 在途;(b) 免费偏好对——II-6 rollout 中学生做
   错的题,(错解, teacher 正解) 天然构成 DPO/ORPO 对,零额外 teacher
   成本,hard-label 约束不挡。SFT 后加一小段偏好阶段。
3. 方差:种子摆动 ±.08(2B),checkpoint averaging(探针最优 checkpoint
   平均,免费)直接服务"稳定明显赢"。
验证台子:gsm 外部评噪声 ±.034,测不出 <.05 的改进;新环节主战场
AppWorld,gsm 只做 sanity。

## 升级波首批判定(8/17 深夜,2B×20k×外部 200 题)
- **偏好对(E3_PREF)方向为正**:ourscorr+pref .670/.675/.620(均值 .655)
  vs 冻结 ourscorr 同种子 .633/.625/.610(均值 .623)——**配对 3/3 种子全升,
  +.032**。探针保险三种子均未触发回滚(降幅 ≤ 一题)。这是 Stage III 第一个
  方向为正的干预:与既往失败(重排/重权同一批 token)的区别 = 引入了 SFT
  token 不携带的新信号(压低学生自己的错误解法模式)。零额外 teacher 成本。
  待 avg/bs 对照落地后确认无 behav-stop 混杂(同跑 boot3 .642 ≈ 基线,初步排除)。
- **草稿桥(E3_SKETCH)在 gsm 上中性**:boot3+sketch .600/.580/.685(均值
  .622)vs 冻结 boot3 ≈ .630 —— 草稿花 ~4.6% 预算没换来 in-task 增益。
  候选解释:30-token 开头对 gsm 太短(解题代码的技能内容在中后段);或
  岭桥聚合近似在 u(x) 求和层面已够用(与 boot5/7 结论一致)。留作消融;
  若在 AppWorld(长轨迹、技能异质)上再试可加长草稿至 60-100 token。
- samp 三跑死因是实现 bug(selfamp 自产行缺 _grad_score → selection_stats
  KeyError),已修复重提(tc-samp4)。前两跑死于组配额,该 bug 是潜伏的
  第三层;修复后 samp 判定第四次尝试。
- avg(权重平均)与 bs(行为停对照)×3 种子仍在队列。

## 权重平均判定(8/18 凌晨)——第二个真增益,方差腰斩
- boot3+行为停+权重平均(avg):外部 .655/.665/.680,**均值 .667,极差仅 .025**
  (对比历史 boot3 种子摆动 ±.08)。
- boot3+行为停(bs 对照):.625/.620(s2 在途)≈ 冻结 boot3 .630 → 行为停本身
  中性,增益全部来自 checkpoint 平均。配对 +.030/+.045。
- 机制:三种子全部"adopted mean of 5 checkpoints",探针无损。
- 战略意义:均值 .667 已略超最强基线带(selfinst ≈ .658),且**方差从 ±.08 收到
  ±.013** —— "稳定明显赢"的两个成分(稳定、赢)同时改善,零成本。
- 组合验证已排(tc-combo ×3):全臂统一 行为停+权重平均(基线也享受同样训练
  升级,公平对照),ourscorr 额外加偏好阶段 —— 检验我们的采购+偏好在训练侧
  拉平后是否仍领先。

## bs_s2 + samp 终判(8/18 晨)
- bs 三种子齐:.625/.620/.580(均值 .608)< 冻结 boot3 .630 → 行为停单独用
  方向略负;avg 配对增益修正为 +.030/+.045/+.100(均值 +.058)——平均不仅
  降方差,还救回行为停的损失。
- **selfamp(自放大)终判:中性**。ourscorr+selfamp .605/.675 vs 冻结 .633/.625,
  配对 -.028/+.050,无一致方向 → 不进 v1.1,留消融。
- **对比要点(论文级)**:同一次免费 rollout,**错题当偏好负例 +.032(3/3),
  对题当额外 SFT 正例 ±0** —— 学生自产数据的边际价值在负例:错题携带 SFT
  语料之外的新信息(学生的失败模式),对题只是复读已付费示范已覆盖的内容。

## combo 判定(8/18 上午)——公平对照下仍然领先,v1.1 证据落定
全臂统一 行为停+权重平均(基线享受同样训练升级),ourscorr 另加偏好 v1:
- selfinst+avg .635/.655/.660 = .650(spread .025)
- boot3+avg .635/.680/.685 = .667(复现独立 avg 波的 .667)
- **ourscorr+avg+pref .665/.670/.670 = .668,spread .005**
要点:① 训练升级拉平后,我们两臂仍高于最强基线 +.017~.018;
② ourscorr+pref 对 selfinst **配对 3/3 全胜**(.665>.635, .670>.655, .670>.660);
③ spread .005 = 全场最稳。"稳定明显好"两成分达成:每种子都赢 + 方差最小。
v1.1 = v1 + 偏好阶段 + 权重平均,证据链闭合;v2 偏好(59 对 vs 4 对)在途。

## 偏好过量与探针盲区(8/18 中午,sanity 级发现)
- v2 挖矿本身成功:59 对 vs v1 的 4 对(15 倍),68 近失负例 / 43 崩溃被滤,
  覆盖 13 atom。
- 但 0.8B 上 30 步 ORPO:可运行率探针恒 1.00,正确率崩至 .30(同跑基线
  .50-.57)——**偏好压力安静过量,"能跑"探针对"算对"失明**。
- 修复:① 偏好保险改用 cal 正确率探针(cal 行带 teacher 答案,金标免费),
  已验证能抓崩回滚(.47→.33 触发);② 剂量上限。0.8B 连 12 步也过量
  (被保险拦下),v1 的 +.032 来自 ~4 步。
- B200 剂量对照已提交:combo2(6 步)vs combo2b(12 步)× 3 种子,与
  combo(v1 对,2-4 步)配对读剂量-反应曲线。
- 论文素材:免费负例的价值窗口——太少欠拟合失败模式,太多摧毁生成
  正确性;正确率探针 + 剂量上限 = 安全使用免费偏好信号的两道护栏。

## 剂量对照初判(8/18 晚,combo2 全 + combo2b_s0)
- combo2(v2 对 41-56 个,6 步):ourscorr .615/.660/.650 = **.642** < combo
  (v1 对 ~4 个,2-4 步).668。s0 cal-acc .73→.67(保留但下滑),ext -.05。
- combo2b_s0(12 步):cal-acc .60→.47 → 保险回滚,ext .655 ≈ 无偏好态。
- **初判:放大免费偏好对无增益,v1 小剂量仍是最优配置**。假说:贪心解码的
  失败 = 学生推理时真正会犯的"模态错误",信息密度高;温度采样的错误长尾
  是学生实际很少产生的噪声,压它们只是扰动策略不获利。
- 诚实记录(方法论):combo2 的 boot3 臂(.630 均值)与 combo 同配置
  (.667)差 .037 —— 同种子跨 run 存在 ±.03-.07 的 CUDA 级不可复现噪声;
  **跨 run 配对种子比较不可靠,应依赖 3 种子均值 + 同 run 内对比**。此前
  两次独立 avg 波 3 种子均值都是 .667(均值层面可复现)。
- 存储:组 blue 扩至 18T(现用 7.9T,另有 20T orange)——配额危机解除;
  工作区留在部门空间不迁回,orange 备作旧结果归档。

## 剂量曲线收官(8/18 深夜,combo2b s1/s2)——孤立偏好阶段判死刑
2B×20k 外部 200 题,v2 对 ×12 步(combo2b):s0 回滚 .655;s1 cal 探针
恰降一题(.67→.60,等于阈值不触发回滚)→ 保留 → **外部 .31 灾难**;
s2 降三题触发回滚 → .66。完整剂量-反应:v1@4 步 +.032 / v2@6 步 -.026 /
v2@12 步 = 回滚或灾难。
**两个结论**:① 孤立偏好阶段在 2B 上 12 步即入危险区,且 15 题 cal 探针
的粒度不足以稳定拦截(一题之差掩盖了 .35 的崩塌)——护栏有极限,
结构性修复(统一目标)是正路;② 这条剂量曲线本身就是统一 ORPO 的
动机实验:分阶段无锚 → 剂量敏感 + 护栏粒度受限;共训练锚定 → 剂量
问题结构性消失(0.8B unisan 已示例:同负例孤立 4 步崩、统一全程稳)。
tv(39651427)/uni(39651428)2B 终审波已在 B200 排队。

## tv/uni 首轮终审与整改(8/19 凌晨)
- **tv(双视图字典)转正,判据要看全指标**:外部 .680/.645/.610(≈ ridge
  版 .667,跨 run 噪声内),但中间量全面占优——浪费减半(~2.0k vs
  ~3.1k token)、入库 +10%(152-156 vs 138-146 行)、**未满足需求减半**
  (8.6k vs 18.3k)、多样性略好(emb 相似 .786 vs .802)。判定:同精度、
  半浪费、双倍需求覆盖、无桥 → 进方法主线;浪费与覆盖本就是一等指标。
- **uni_s0 偏低(.605)机制定位**:103 个 rollout 失败只有 8 个可解析成
  负例;8 对 × 8 epoch ≈ 分段版 4 倍剂量,且从 epoch 1 就施压——chosen
  似然未学会时位移最重(分段版的隐性优势 = 只在 SFT 收敛后压)。
- **整改(保统一、无新常数)**:E3_UNIGATE——odds 项按行门控,仅当该行
  正解 NLL 低于全体行运行均值时激活("先学会,再区分")。0.8B 压力
  测试在跑(v2 的 59 对 + 门控);过则 uni2×3 上 B200。

## 门控统一目标终审(8/19 晨)——统一形态转正
- uni2(NLL 门控):.645/.655/.680 = .660;对无门 .630 配对 3/3 全胜
  (+.030);对分段式 .668 差 .008 = 平局。机制链三步全有数据:
  无门(早期施压)负 → 加门恢复 → 与最优分段打平。
- 0.8B 压力测试系列同向:孤立 .300-.467 / 无门统一 .533 / 门控统一
  .600(系列最高)。
- 判定:Stage III 采用门控统一目标(单目标 + 目标内 NLL 门,无阶段无
  剂量旋钮),分段版降为消融。
- gateoff_s0 首读:tv+门全开 .620 vs tv 带门 .680——开门疑似反伤
  in-task(待 s1/s2);若坐实,门为"免费纯度 + in-task 保护"双重角色。

## v1.2 全判定收官(8/19 晨,15 格全落地)
- **gateoff 三种子齐:门的 in-task 代价 ≈ 0**(开门 .642 vs 带门 .645;
  s0 的 -.06 是噪声)。结论:**零泄漏免费**——门保留,in-task 不付费,
  off-task 残留 0.000 的独特证据保住,预算效率故事成立。
- **tvuni(完整 v1.2)**:ourscorr .650/.640/.640 = .643,极差 .010
  (全场最稳);boot3 .627;selfinst 同跑 .648。
- **大局(2B×20k gsm)**:所有顶配(分段 .668 / 门控统一 .660 / tvuni
  .643 / selfinst .648-.670)落在 .64-.67 一个统计平局带内;我们的臂
  以最小方差(.010)+ 零泄漏 + 半浪费与最强基线并列。gsm 的 in-task
  上限已被任务饱和封死——赢面战场按既定计划在 AppWorld。
- **方法定稿 v1.2**:tv(双视图字典:同精度、半浪费、双倍需求覆盖、
  无桥)+ 门控统一目标(同精度、无阶段无剂量旋钮)+ 权重平均 + 归属门
  (免费纯度)。分段偏好、ridge 桥、无门统一全部降为消融,各有机制
  数据。

## AppWorld 战役第一日(8/19)——机制三连
1. **harness bug 真实但边缘**:合并模型丢 eos → 生成冲过回合边界幻觉
   后续轮次;修复(显式停止 token)后幻觉清零、个别任务 24→4 步,但
   聚合分几乎不动(atoms .197→.202)。
2. **数据量假设否定**:全池 2506 行(~125×)= .197 ≈ 20k 的 .202,
   均低于 base .277。示范克隆不因加量而好转。
3. **机制定论:风格捕获**——teacher 示范纯代码零解说,SFT 磨掉 base
   的任务级规划(轨迹:开场反射执行示范动作、无任务分析);
   agent 任务上示范克隆式蒸馏 < 学生零样本(论文级发现)。
4. 协议教训:温度 0 下单任务两评仍漂(3/3 vs 0/6)——AppWorld 结论
   只看聚合 + 多种子。
→ 方法主场:v1.2 阶段 III 分层(自解保留 + 失败纠错)机制上正中此
  病;明日主实验 = attempt-first+门控统一 vs 纯 SFT 克隆。9B 列在途。
