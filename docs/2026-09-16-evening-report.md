# 2026-09-16 晚间报告:ALFWorld 有效主干、HotpotQA 支持集扩充、FSCD v1 启动

> **本文已被 `docs/2026-09-17-overnight-report.md` 取代**,保留为按时间顺序的原始记录。
> 状态:草稿,随节点更新。时间为 America/Chicago。所有数字来自本仓库与 LONI 上的账本与作业产物,路径在各节标注。

## 0. 三句话

1. **ALFWorld 这一列此前从未被有效训练过,而一旦有效训练,纯 CE 会大幅损伤学生**(base 55.71 → 36 题 27.86 / 71 题 40.71);
   零训练 2×2 排除了"监督目标与部署协议错配"这一解释,原因指向训练本身(材料窄、剂量、执行行为)。
2. **预算口径按用户要求改为"教师被查询的 support 任务比例"**,并以原始训练集百分比锚定;HotpotQA 为此补购了 1,609 个新父任务(冻结顺序前缀)。
3. **FSCD v1(跨父任务示范复用与内化)按新协议启动**:数据冻结与上下文构造(T1)已交付验收,FIT-only 纯 CE 主干(T2a)正在训练。

## 1. ALFWorld 有效主干

### 1.1 为什么重跑

9/15 发现归档 Table 1 的更新器是空操作(逐格审计:trained−init CE ∈ [−0.009, +0.002] nats,KL ≤ 0.001),
因此 ALFWorld 三行基线从未被训练。本轮用验证过的 AdamW 1e-5 重跑主干:base + 正常纯 CE(按 token 比例),LoRA 16/32,3 遍行曝光,
渲染/终止监督/曝光检查沿用原管线,评测 valid_seen 140、贪心、40 步上限。

### 1.2 结果

截至 2026-09-17 00:00,全部格子(同一 runner、AdamW 1e-5、LoRA 16/32、按 token 的纯 CE、valid_seen 140 贪心 40 步):

| 格 | 部署 | support 题 | 示范 | 训练行 | 步数 | 遍数 | EM | 胜/140 | 跑满 40 步 | Δbase |
|---|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| base | ReAct | — | — | — | — | — | 55.71 | 78 | 62 | — |
| base | 命令模式 | — | — | — | — | — | 54.29 | 76 | 64 | — |
| 纯 CE | ReAct | 36 | 22 | 346 | 9 | 1 | 58.57 | 82 | 58 | +2.86 |
| 纯 CE(种子 1) | ReAct | 36 | 22 | 346 | 9 | 1 | 58.57 | 82 | 60 | +2.86 |
| 纯 CE | ReAct | 36 | 22 | 346 | 26 | 3 | 27.86 | 39 | 101 | −27.86 |
| 纯 CE | 命令模式 | 36 | 22 | 346 | 26 | 3 | 25.71 | 36 | 104 | −28.57 |
| 纯 CE | ReAct | 71 | 49 | 726 | 19 | 1 | 57.14 | 80 | 60 | +1.43 |
| 纯 CE | ReAct | 71 | 49 | 726 | 55 | 3 | 40.71 | 57 | 83 | −15.00 |
| 纯 CE | ReAct | 142 | 104 | 1,535 | 20 | 0.5 | 55.00 | 77 | 63 | −0.71 |
| 纯 CE | ReAct | 142 | 104 | 1,535 | 39 | 1 | 22.86 | 32 | 108 | −32.86 |
| 纯 CE | ReAct | 142 | 104 | 1,535 | 77 | 2 | 52.14 | 73 | 67 | −3.57 |
| 纯 CE | ReAct | 142 | 104 | 1,535 | 116 | 3 | 57.86 | 81 | 59 | +2.14 |
| 纯 CE(种子 1) | ReAct | 142 | 104 | 1,535 | 116 | 3 | 61.43 | 86 | 54 | +5.71 |

**按优化器步数排开是一条 U 形曲线**:9 步 +2.86、19 步 +1.43、20 步 −0.71、26 步 −27.86、39 步 −32.86、55 步 −15.00、77 步 −3.57、116 步 +2.14/+5.71。
固定材料(142 题)内部同样非单调,而且谷底窄而深:20 步基本持平、**39 步崩到 −32.86**、77 步已回到 −3.57、116 步转为 +2.14/+5.71。因此:

- "剂量(遍数)是主因"要收窄成"**优化器步数区间**是主因,而且非单调";
- "材料越多同样遍数越安全"**已撤回**(142 题在 39 步比 36 题在 26 步更惨);
- 主表 ALFWorld 列"正常训练纯 CE"应填 **full support(142 题)3 遍两种子 57.86 / 61.43,均值 +3.94**,并注明种子极差 3.57 与 140 题评测的抽样不确定性。

未验证的机制猜想(待行为探针检验):学生要从多行 ReAct 回复过渡到教师那种"第一行一个裸命令"的策略,中途经过一个两种格式都不成形的区间;
证据是 base 在命令模式下本就有 54.29,且崩塌格跑满 40 步的局数从 62 升到 101–108。

### 1.3 零训练 2×2:部署协议不是原因

| 模型 | ReAct 部署(≤256 token) | 命令模式部署(≤32 token) |
|---|---:|---:|
| base | 55.71(78/140) | 54.29(76/140) |
| 裸命令纯 CE(36 题 3 遍) | 27.86(39/140) | 25.71(36/140) |
| **CE − base** | **−27.85** | **−28.58** |

在命令模式下,模型的输出格式**正好是它被训练的格式**(第一行一个裸命令),退化幅度与 ReAct 部署下相同。
因此"监督目标与部署协议错配"不是主因,先前的归因已撤回。

### 1.4 材料的性质(收紧后的表述)

ALFWorld 的 luna 银行**没有可见的推理监督**:采集提示是 "Reply with exactly one admissible command and nothing else",
`behaviors[i].text` 就是裸命令。不输出 Thought 不等于教师没有推理,因此不能把退化归因于"不再思考"。
对照 HotpotQA 的 react7 银行监督的是完整回合(`Thought n: … / Action n: …`),同一套纯 CE 在那边是正增益。

解析器事实(`src/alfworld_eval.py::pick_command`):只取回复**第一行**,做完全/大小写/双向子串匹配,失败回退 `look`。
`ACTION: <命令>` 可被接受;`THOUGHT: …` 开头的多行回复会被判成 `look`。教师侧另有取最后一个 `ACTION:` 的解析,学生侧没有。
**若要重采完整 ReAct 示范,必须同时修改学生侧解析器。**

## 2. HotpotQA 支持集扩充(补购)

### 2.1 口径

预算 = **教师被查询的 support 任务数**(每题最多 3 次尝试,取第一条通过验证的轨迹),按原始训练集百分比锚定;实际花费作为结果报告,不作为旋钮。

### 2.2 本轮采购

| 项目 | 数值 |
|---|---:|
| 冻结顺序中新查询的父任务 | 1,609 |
| 验证成功 | 1,067(66%) |
| 教师真实失败 | 481 |
| **我方基础设施失败(Wikipedia 限流)** | **61** |
| 计费输出 token | 1,174,631 / 1,210,000(97%) |
| 加上原 support-200 的 149,169,HotpotQA 教师累计 | 1,323,800 |

支持集按"冻结顺序 + 跳过基础设施失败的题"来数:**1%(904 题 = 200 + 704)完整可用**,其中验证 614(68%,与原 support-200 的 136/200 一致);
2%(1,809 题)只凑得出 1,548 题,差 61 题(补齐约需 45k token,超已批上限约 4%)。

材料已合并为 `envs/hotpotqa/teacher_pool_scan/r7_s904`(904 题 / 1,606 次尝试 / 614 验证 / 665,173 计费 token)与银行 `data/rtd/v1_1_hotpotqa_luna_s904`,
与 ALFWorld 使用同一个预算化 runner,便于两个基准逐项对齐。

### 2.3 限速与代价(工程)

采购初始只有 3.3 次尝试/分,瓶颈是令牌桶按每请求预留 2,048 token(桶 50k/分 → 24 请求/分),而实际每请求仅用 50–150 token;加 `BFAS_TEACHER_TPM` 覆盖后解开。
随后撞上 Wikipedia 限流:32 并发 39% 尝试失败、10 并发 32%、**4 并发 0%**。61 个永久损失的父任务由此产生,单列为本方基础设施失败。

## 3. FSCD v1

### 3.1 T1 数据冻结与上下文(已验收)

- 划分:FIT 128 / SELECT 36 / CHECK 36,按 `SHA256("fscd-v1-split|"+parent_id)` 升序;我独立复算与工具输出一致,三集互斥且并集等于 support-200。
- EVAL-200:自官方 dev 排除 support-200、eval split、两个 confirmation split、scan pool 2000、补购清单与同题重复后按 `SHA256("fscd-v1-eval|"+…)` 取前 200;与上述每一个集合的交集均为 0。
- 银行回执:FIT 侧 276 个回合 / 11,980 个监督 token / 83 个有验证轨迹的父任务;历史教师费用 149,169 输出 token、1,503,277 输入 token。
- 上下文:示范池 83 条(全部来自 FIT,中位 504 token);Q1 两复本 951 / 906 token(无共享父任务),Q2 两复本 890 / 893 token(共享 1 个父任务,已如实标注);覆盖度 Q2 0.906 > Q1 0.867;包裹文字逐字一致。

### 3.2 T2a 主干(进行中)

276 行、11,980 目标 token、75 步(3 遍 × 25)、328 个 LoRA 模块;断言全过(round-trip、FIT-only、每遍每行恰好一次、原生边界进监督、尾批保留)。
训练在 LONI 完成(75/75 步):固定 FIT 批 CE **1.2363 → 0.2736**,LoRA 净位移 l2 2.37(656 个张量 = 328 模块 × 2),verify 通过。
过程中两处工程问题已记录并修好:工具按字节比较 base_model 清单(含宿主路径元数据),跨主机永远对不上 → 改为在训练主机本地重建行并断言计数一致;
verify 拿 PEFT 保存的 7 个短名去比 328 个全路径模块名 → 改为比较末段集合并校验张量成对。

## 4. 工程事故与纪律(如实记录)

- **采购限速**:令牌桶按每请求预留 2,048 token、桶 50k/分 → 24 请求/分封顶,而实际每请求只用 50–150 token;加 `BFAS_TEACHER_TPM` 覆盖后解开。
- **Wikipedia 限流**:32 并发时 39% 的尝试失败、10 并发 32%、4 并发 0%。我的高并发试探使一批父任务三次尝试全部耗尽在限流上,**这是本方的基础设施损失,不是教师能力失败**,单列记账。
- **pkill 自匹配**:再次用 `pkill -f <含自身模式>` 杀进程,连带杀掉自己的工具 shell 与监视器(exit 144)。已改为先查 pid 再逐个 kill。
- **跨主机审计**:FSCD 主干工具按字节比较 base_model 清单(含宿主路径元数据),rai 上生成的审计在 LONI 永远对不上;作业改为在训练主机本地重建行并断言计数一致。
- **部署纪律**:LONI 上正在训练的格子,其评测阶段会重算 source_hashes;**训练期间不得覆盖部署树的源码**,否则几小时的训练会在评测阶段失败。

## 5. HotpotQA 有效主干(support-904 = 训练集 1%)

同一个预算化 runner、同一个优化器,评测 dev 500 题(EM 主指标,F1 辅助):

| 格 | support 题 | 可用轨迹 | 训练行 | 步数 | 遍数 | EM | F1 | Δbase |
|---|---:|---:|---:|---:|---:|---:|---:|---:|
| base(同批) | — | — | — | — | — | 37.60 | 0.477 | — |
| 纯 CE | 904 | 614 | 2,079 | 52 | 1 | **38.60** | 0.492 | **+1.00** |
| 纯 CE | 904 | 614 | 2,079 | 156 | 3 | 跑中 | — | — |

3 遍那一格是按今晚 ALFWorld 的教训补的:在那边 EM 随步数非单调,所以 HotpotQA 也必须实测,不能沿用"3 遍更好"或"1 遍更安全"的任何一条。

## 6. FSCD v1 阶段 1(通过)与阶段 2(进行中)

阶段 1(零训练,只改提示,冻结主干 θU):

| 集合 | Q0 | Q1(随机跨父任务示范) | Δ EM | 95% 父任务 bootstrap CI | 胜/负/平 |
|---|---:|---:|---:|---|---|
| SELECT(36) | 0.583 | 0.694 | +0.111 | [+0.028, +0.208] | 5 / 0 / 31 |
| CHECK(36,一次性) | 0.472 | 0.597 | **+0.125** | **[+0.028, +0.222]** | 8 / 1 / 27 |
| Q2(覆盖度选择,SELECT) | — | — | −0.042 | [−0.153, +0.083] | 2 / 5 / 29 |

四项门槛全过(select_gain / check_gain / replicas_nonnegative / no_systematic_condition_difference),诊断全干净,Q0 重复性 16 个父任务 0 次翻转。
**覆盖度选择为负,不得宣称我们的选择规则有贡献;有效的是最朴素的随机示范。**

阶段 2 已按协议启动:固定状态池 |Z| = 10,594 个 token 位置(124 个 FIT_QUERY 父任务,冻结参考训练前后哈希一致、训练期间不刷新),
CE / SELF / CTX × 两个批次种子共六臂并行训练中,随后在 EVAL-200 上一次性评 7 个模型,**三臂部署时都不带任何新增示范**。
两项主比较是 CTX − CE 与 CTX − SELF,按父任务 bootstrap 10,000 次、seed 271828,主比较报 97.5% 区间(近似 Bonferroni)并同时给 95% 区间。

## 7. 夜间执行与自动化(如实记录)

三条守候链路让流水线在无人干预下推进,每条都带失败保护:
1. **状态池 → 六臂**:状态池作业 COMPLETED 才提交六臂;非 COMPLETED 则不提交并记录原因。
2. **六臂 → 七个最终评测**:逐个核对六臂的 sacct 退出状态,任一 FAILED/CANCELLED/TIMEOUT 就拒绝提交最终评测。
3. **ALFWorld 扫描结束 → 行为探针**:等部署冻结解除后才覆盖部署树,再对 base / 39 步崩塌格 / 116 步恢复格 各跑 8 局并 dump 每回合原始回复。

阶段 2 的六臂审计(全部通过):每臂 75 步;θ=θU 时 SELF 的 KD 项为零、CTX 非零(协议要求的正常性质);冻结教师参数训练前后哈希一致;
固定状态池 |Z| = 10,594 个 token 位置、训练期间不刷新;审计如实记录三臂算力不对等。

本轮明确不做、也不假装做的事:没有把复用银行说成零成本;没有把固定状态池说成 on-policy;没有为通过门槛而增加样本或换候选;
覆盖度选择为负就写成负,不给它任何解释性补救。

## 8. FSCD v1 第二阶段:最终结果(EVAL-200,一次性打开)

七个模型,全部在原生 harness 下评测,**三臂部署时都不带任何新增示范**:

| 模型 | 种子 0 | 种子 1 | 完成率(种子 0/1) |
|---|---:|---:|---|
| 主干 θU | 0.425 | — | 0.815 |
| CE(继续纯 CE) | 0.415 | 0.415 | 0.840 / 0.790 |
| SELF(CE + 自蒸馏) | 0.430 | 0.420 | 0.830 / 0.810 |
| CTX(CE + 示范条件化蒸馏) | 0.420 | 0.425 | 0.825 / 0.770 |

两项预注册主比较(按父任务 bootstrap 10,000 次,seed 271828):

| 比较 | Δ EM | 97.5% CI(近似 Bonferroni) | 95% CI | 逐种子 |
|---|---:|---|---|---|
| **CTX − CE** | **+0.0075** | [−0.0275, +0.0425] | [−0.0225, +0.0375] | +0.005 / +0.010 |
| **CTX − SELF** | **−0.0025** | [−0.025, +0.0225] | [−0.0225, +0.020] | −0.010 / +0.005 |

辅助:CE − 主干 −0.010;SELF − CE +0.010;CTX − 主干 −0.0025。

**判读(协议 §8.3):工具判 row 6「不算通过」**(优势只在部分种子上出现),实质与 row 4 相同 ——
**第一阶段的上下文辅助收益成立,第二阶段的"可内化"未获支持。**

必须同时写清楚的限制:
- **区间跨零 = 未检出,不等于等价**;EVAL-200 不保证分辨 1–2 pp 的效应;
- 本轮**没有**为把结果做成显著而扫系数、加样本或换候选(协议明令禁止);
- 覆盖度选择在第一阶段为负,不为它做任何解释性补救;
- 工具自查出一处不达标:主干阶段没有写 GPU 账本,因此**不能宣称完全满足协议**。

成本:历史教师 149,169 输出 + 1,503,277 输入 token(343 次 rollout);本轮额外 GPU 17.53 小时;1,828 条完整 rollout 记录。

**这条线的净结论是一个干净的两段式答案:跨父任务的交互示范放进提示里,对冻结学生确有帮助(+0.111 / +0.125,四项门槛全过);
把这个收益蒸馏进参数,则在本轮的规模与协议下没有可检出的收益。**

## 9. 行为探针:确认了终点,否掉了过渡区猜想

对三个 checkpoint 各跑 8 局并 dump 每一回合的原始回复(成功率样本太小不可用,只读格式统计;190–221 步):

| 模型 | 写 `ACTION:` | 首行即合法命令 | 重复上一命令 | 回退 `look` |
|---|---:|---:|---:|---:|
| base | 0.919 | 0.000 | 0.217 | 0.068 |
| 39 步(崩塌) | 0.911 | 0.000 | 0.232 | 0.058 |
| 116 步(恢复) | **0.000** | **0.889** | **0.051** | 0.111 |

- **确认**:116 步的策略已完全切换为教师的裸命令风格,并且循环显著减少。
- **否证**:39 步的崩塌格在格式统计与原始回复上都与 base 无异,因此"卡在两种格式之间的破碎区"不成立。

**截至本轮,ALFWorld 崩塌的成因仍未确定。** 已被直接证据排除的解释有四个:
监督/部署格式错配(命令模式 2×2)、材料缺少可见推理(1 遍即 +2.86)、材料规模(142 题在 39 步最惨)、过渡区格式破碎(本探针)。
剩余候选是决策质量本身下降,或与解析器"只取第一行 + 双向子串匹配"的系统性交互;两者都需要新实验,本文不选边。

### 9b. 解析器交互假说也被否掉

用同一批 dump 离线核对"模型自己声明的动作"与"解析器实际执行的命令"是否一致:

| 模型 | 回合数 | 写出 `ACTION:` | 其中被解析器取错 | 触到 token 上限 | 回复长度中位数 |
|---|---:|---:|---:|---:|---:|
| base | 221 | 0.919 | 0.069 | 0.086 | 104 |
| 39 步(崩塌) | 190 | 0.911 | **0.058** | 0.095 | 103 |
| 116 步(恢复) | 216 | 0.000 | — | 0.000 | 6 |

崩塌格的取错率比 base **更低**(0.058 vs 0.069),长度、截断率也基本一致。
**因此"只取第一行 + 子串匹配"的解析器交互不是崩塌的成因。**

至此 ALFWorld 崩塌已排除五种解释:监督/部署格式错配、材料无可见推理、材料规模、过渡区格式破碎、解析器取错。
在格式、解析保真度与生成预算都与 base 相同的前提下,39 步模型只是**决策变差**。成因定位到决策质量本身,这一步不需要再做格式类实验。
