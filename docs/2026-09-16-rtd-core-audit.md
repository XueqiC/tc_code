# 旧 RTD 核心增量复核:盘点(2026-09-16 14:23 CDT,零 GPU、零教师)

用户 14:17 CDT 指令:冻结 HotpotQA 新机制线;转向"旧 RTD 核心增量复核",一天只回答一个问题:同骨干、同材料、同预算、正常训练的强 CE 对照下,
RTD 的核心模块有没有可重复增量?四行:正常训练纯 CE / RTD 完整候选 / 关闭核心模块 / 权重置换对照。优先复用现有 checkpoint、冻结银行、逐题结果;先核对原始正向结果,不换学生、基准、预算。

## 1. "原始正向结果"是什么,现在在哪

| 版本 | 配对 | 结果 | 对照 | 状态 |
| --- | --- | --- | --- | --- |
| RTD v1.0(BFCL,sealed_replay,9/7–9/9) | Qwen3.5-4B + luna | hpg 三臂终点 R0 46.75 / R1 47.31 / R1s 46.53,base 46.06 | 无强 CE;三轮极差 0.78 | 用户已判"无信号(协议原因)";学生已退役 |
| **RTD v1.1(BFCL,9/9)** | Qwen3.5-4B + luna | **10% 点:V0(随机采购,固定 a=.5)47.08 / V1 45.97 / V2(联合代理采购,学 α+d)46.35,base 46.06** | 无等曝光强 CE;单种子;±1.3 单种子噪声带 | 9/9 15:35Z 进过 Table 1(V2 46.4†)与消融表;25% 点未跑(hpg 账号暂停);rai 重跑(v11c)只有 manifest/compute,**没有评测输出**;论文现把它放在附录"earlier pair" |
| unified P1 / v1.1 luna(ALFWorld、BFCL,9/11) | gemma-4-12B + luna | 无 | — | ALFWorld V0 11 h 未提交第 1 步后中止;BFCL V0 中止;D3 只有冒烟;**当前配对上 RTD 家族没有任何跑完的臂** |
| 机制验证 C vs D(BFCL,9/12–13) | gemma + luna | D−C +1.37 [−1.00, +3.77] | C = 通用练习 | 空结果 |
| 机制线 I/P/L、读取器、四格(HotpotQA,9/13–14) | gemma + luna | 空结果 | — | 且用的是空操作更新器(9/15 发现),作为训练方法比较无效 |
| **stage-2 v5/v6(HotpotQA react7,9/15,AdamW 有效)** | gemma + luna | 带 KL 锚:SmartAD 41.4 > SAD 40.0 > SFT 39.4 = FIXSEG > MECH 38.4 > META 38.2 > PERM 37.6(同批 base 35.8);**无锚公共目标 λ=0:U0(纯 CE,按 token)两种子 40.5,+3.5±1.1(同批 base 37.0);F0(SmartAD Eq.5 固定分段)40.3;分配效应 +0.1±0.5** | 有:同批 base、两配对种子、逐题结果、冻结银行(react7 136 验证 / 461 回合) | 机制臂(MECH/PERM/信任域 TR)**从未在 λ=0 上跑过**;MECH 权重来自饿死的反馈试点(权重塌缩到单片段、与长度相关 1.00);探针检验"不可判定" |

结论一:**唯一进过表的 RTD 正向结果(v1.1 BFCL 10% 点)在已退役的 Qwen 学生上,单种子、无强 CE 对照、差异在噪声带内,checkpoint 在暂停的 hpg 账号上;
它不能靠"复用现有 checkpoint"复核,复核了也进不了主表。** 论文主表(gemma 配对)的 RTD 行全部是"·"。

结论二:当前配对上,同时具备"正常训练(AdamW)的强 CE 对照 + 同材料 + 同预算 + 逐题结果 + 冻结银行 + 两种子"的场地只有 HotpotQA react7 冻结根(v6)。
主表里的 Table 1 基线数(SmartAD 58.10 等)仍是空操作更新器的产物;HotpotQA 上 AdamW 重跑的忠实基线只有单种子(SmartAD 41.4 / SAD 40.0,同批 base 35.8),
ALFWorld 没有 AdamW 重跑。

## 2. 一天内能回答的那个问题(建议)

在 HotpotQA react7 冻结根上补齐四行,全部 checkpoint **同批评测**(评测漂移 1–2 pp):

| 行 | 内容 | 复用 / 新跑 |
| --- | --- | --- |
| 正常训练纯 CE | U0(λ=0,w=p)seed 0/1,113 步 AdamW 1e-5 | 复用 checkpoint,同批重评 |
| RTD 完整候选 | 回报驱动分段权重(MECH-λ0 或信任域 TR):反馈用修好的批采样器,T=0.5,只取成功率居中的父任务,≥8 样本/父任务;w(α) 信任域规则 | **新跑**:反馈 1–2 h + 两种子训练 |
| 关闭核心模块 | F0(固定分段比例、无反馈,λ=0)seed 0/1;U0 本身也是 w=p 的"模块关" | 复用,同批重评 |
| 权重置换 | PERM-λ0:同一组权重按片段置换,两种子 | **新跑** |

预计 LONI:反馈 1–2 h → 4 个训练臂并行 1.5 h → 9 个模型同批评测 1 h ≈ 5–6 h;零教师调用;代码路径在冻结树里都有(cr_mechanism_cell 的 MECH/PERM/TR 臂、
hotpotqa_feedback_screen 批采样、stage2_weights),需要一次 Codex 把"反馈 → 权重 → λ=0 臂"接通并用冻结 API 跑测试。

**这检验的核心模块是"回报驱动的分段权重"(9/15 定义的 mechanism),不是 v1.1 的采购/门控(V0 vs V2)。** 若你指的是采购/门控:当前配对上一天做不了
(ALFWorld 一轮 >11 h;BFCL luna 银行只有 20 包;HotpotQA 的 unified 引擎只注册过、没跑过)。

## 3. 请定

- A:按 §2 在 HotpotQA 上跑四行(今天出表)。
- B:先复核退役配对上的 v1.1(需要 hpg 账号恢复或在 rai 重跑 Qwen 三臂;结论不进主表;≥1 天)。
- C:其它(例如先把 ALFWorld 的 AdamW 基线补上,再谈方法行)。
