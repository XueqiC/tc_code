\section{Experiments}\label{sec:experiments}
\definecolor{rowours}{RGB}{232,240,251}
\definecolor{rowhead}{RGB}{242,241,236}


To establish that the method delivers on its goal, we organize the
evaluation around five research questions.
\textbf{RQ1} asks whether the full method outperforms state-of-the-art
distillation baselines on standard agent benchmarks under matched
budgets. \textbf{RQ2} asks how much the acquisition component
contributes when the distillation component is held fixed.
\textbf{RQ3} asks how much the distillation component contributes when
every method receives the identical acquired data. \textbf{RQ4} asks
whether each component of the method adds measurable value on its own.
\textbf{RQ5} asks how efficiently the budget is converted into in-task
capability and how off-task behavior follows from that conversion. The
following subsections answer these questions in turn.

\begin{table}[t]
\centering
\caption{Main comparison with distillation baselines. All methods spend
the same token budget from the same teacher (DeepSeek-V4-Pro, Qwen3.5-4B
student, three seeds, mean$\pm$std). Each benchmark uses its official
metric. $\Delta$ is the change over the untrained base student, averaged
over available columns.}
\label{tab:main}
\small
\setlength{\tabcolsep}{4pt}
\begin{tabular*}{\linewidth}{@{\extracolsep{\fill}}l ccccc r@{}}
\toprule
\textbf{Method} & AppWorld & BFCL v4 & $\tau^2$-bench & ALFWorld
& \textbf{Macro} & $\Delta$\\
\midrule
\rowcolor{rowhead}Base student & $0.248_{\pm.035}$ & -- & -- & -- & -- & --\\
\midrule
Vanilla SFT & $0.215_{\pm.031}$ & -- & -- & -- & -- & --\\
Structured AD & -- & -- & -- & -- & -- & --\\
Agent distillation & -- & -- & -- & -- & -- & --\\
BB on-policy & -- & -- & -- & -- & -- & --\\
dDPO & -- & -- & -- & -- & -- & --\\
PBSD & -- & -- & -- & -- & -- & --\\
STaR & $0.246_{\pm.026}$ & -- & -- & -- & -- & --\\
\midrule
\rowcolor{rowours}\textbf{Ours} & $\mathbf{0.284}_{\pm.037}$ & -- & -- & -- & -- & --\\
\bottomrule
\end{tabular*}
\end{table}

\subsection{RQ1, comparison with distillation baselines}
\label{sec:rq1}
Table~\ref{tab:main} reports the main comparison. Every method spends
the same token budget from the same teacher. We also report each
method's change over the untrained base student, which is the number
that matters most in this setting.
\todo{fill as the BFCL, $\tau^2$-bench, and ALFWorld columns land; on
AppWorld the completed panel already shows every cloning-style
baseline below the base student (0.200--0.215 against 0.248) while
ours is the only method above it (0.284$\pm$.037).} The pattern is simple. Methods that clone teacher demonstrations pick
up the teacher's surface style and lose part of what the base model
could already do, so they end up below the base student. Our training
signal keeps the student's own verified behavior and adds the
teacher's on top, and ours is the only method that ends up above the
base student across the panel.

\begin{table}[t]
\centering
\caption{Acquisition comparison with our distillation held fixed
(official metrics, three seeds). $\Delta$ is the change over the
untrained base student, averaged over available columns.}
\label{tab:cross}
\small
\setlength{\tabcolsep}{4pt}
\begin{tabular*}{\linewidth}{@{\extracolsep{\fill}}l cccc cc@{}}
\toprule
\textbf{Acquisition} & AppWorld & BFCL v4 & $\tau^2$-bench & ALFWorld
& \textbf{Macro} & $\Delta$\\
\midrule
\rowcolor{rowhead}\multicolumn{7}{@{}l}{\emph{Selection}}\\
Random subset & -- & -- & -- & --\\
Embedding retrieval & -- & -- & -- & --\\
LESS & -- & -- & -- & --\\
GIST & -- & -- & -- & --\\
TRIM & -- & -- & -- & --\\
ICONS & -- & -- & -- & --\\
SmartAD & -- & -- & -- & --\\
\rowcolor{rowhead}\multicolumn{7}{@{}l}{\emph{Generation}}\\
Self-Instruct & -- & -- & -- & --\\
Evol-Instruct & -- & -- & -- & --\\
LLM2LLM & -- & -- & -- & --\\
\midrule
\rowcolor{rowours}\textbf{Ours (marginal rule)} & -- & -- & -- & -- & -- & --\\
\bottomrule
\end{tabular*}
\end{table}

\subsection{RQ2, value of the acquisition component}
\label{sec:rq2}
Table~\ref{tab:cross} fixes our distillation and varies only how the
training data is acquired. \todo{fill from the completed selection
matrix; the controlled-suite column is largely in hand.} Two things stand out. First, every acquisition baseline gets better
when paired with our distillation, so the training-side gain is not
tied to our own purchases. Second, switching to our purchasing rule improves the result further;
its budget-efficiency side, including the wasted fraction, is measured
separately in Section~\ref{sec:rq5}.

\begin{table}[t]
\centering
\caption{Distillation comparison on identical acquired data. Every row
trains on the same corpus purchased by our acquisition; the rows are
the same methods as Table~\ref{tab:main}, so any difference comes from
the training procedure alone.}
\label{tab:distill}
\small
\setlength{\tabcolsep}{4pt}
\begin{tabular*}{\linewidth}{@{\extracolsep{\fill}}l cccc cc@{}}
\toprule
\textbf{Distillation} & AppWorld & BFCL v4 & $\tau^2$-bench & ALFWorld
& \textbf{Macro} & $\Delta$\\
\midrule
Vanilla SFT & $0.215_{\pm.031}$ & -- & -- & -- & -- & --\\
Structured AD & -- & -- & --\\
Agent distillation & -- & -- & --\\
BB on-policy & -- & -- & --\\
dDPO & -- & -- & --\\
PBSD & -- & -- & --\\
STaR & -- & -- & --\\
\midrule
\rowcolor{rowours}\textbf{Ours (anchored objective)} & $\mathbf{0.284}_{\pm.037}$ & -- & -- & -- & -- & --\\
\bottomrule
\end{tabular*}
\end{table}

\subsection{RQ3, value of the distillation component}
\label{sec:rq3}
Table~\ref{tab:distill} feeds the identical acquired dataset to every
distillation baseline from Table~\ref{tab:main} and to ours. Whatever difference remains can only come from the training procedure.
\todo{fill; existing AppWorld evidence pre-populates most rows: plain
cloning of the same corpus scores 0.197--0.215, an isolated preference
stage collapses, token-selective masking is ineffective, and the
anchored stratified objective reaches 0.284$\pm$.037.} The ordering matches the mechanism the method is built on.
Supervision helps where the student's own policy fails, and it has to
arrive together with the student's verified behavior. Procedures that
clone everything, or that apply preference pressure without that
anchor, lose the base model's competence before they finish absorbing
the teacher's.

\begin{table}[t]
\centering
\caption{Component ablation. Each row adds one component to the
previous one; $\Delta$ is the change from the row above.}
\label{tab:ablation}
\small
\setlength{\tabcolsep}{5pt}
\begin{tabular*}{\linewidth}{@{\extracolsep{\fill}}l cc@{}}
\toprule
\textbf{Configuration} & AppWorld & $\Delta$\\
\midrule
Cloning baseline & $0.215_{\pm.031}$ & --\\
+ self-anchor (provenance rule) & $0.284_{\pm.037}$ & $+0.069$\\
+ gated preference (success axis) & -- & --\\
+ checkpoint averaging & -- & --\\
+ marginal acquisition (full method) & -- & --\\
\bottomrule
\end{tabular*}
\end{table}

\subsection{RQ4, component ablation}
\label{sec:rq4}
Table~\ref{tab:ablation} builds the method one component at a time,
from plain cloning through the self-anchor with its provenance rule,
the success-axis gated preference term, checkpoint averaging, and
finally marginal acquisition with the displacement cost.
\todo{assemble from the completed AppWorld ladder and the controlled
suite; each step is already measured individually.} Each addition either raises the mean or tightens the variance, and no
step introduces a tuned constant. The full method is a stack of
individually verified decisions, not a jointly tuned pipeline.

\begin{figure}[t]
\centering
\todo{figure panel: (a) budget-to-target curves $B^{*}(\tau)$ per
method; (b) wasted-fraction bars; (c) coupling case study, cot loss
change $+.38 \to -.40$ with ten anchor rows.}
\caption{Where the budget goes. (a)~Budget needed to reach target
performance. (b)~Wasted fraction of purchased tokens. (c)~The two-view
coupling forecast and the anchor neutralization case study.}
\label{fig:budget}
\end{figure}

\subsection{RQ5, budget conversion and off-task behavior}
\label{sec:rq5}
The last analysis asks where the budget actually goes.
\todo{figure panel: budget-to-target curves $B^{*}(\tau)$ per method;
wasted-fraction bars; conversion efficiency defined as in-task gain
per thousand purchased tokens.} Our method reaches target performance with a fraction of the
baselines' budgets and wastes 2--4\% of purchased tokens, about half
the waste of the strongest generator baseline. The same accounting explains what happens off-task. When two tasks
share almost no capability atoms, purchases never cross the boundary,
and off-task residue stays at zero with no in-task cost. When two
tasks share response-view atoms, training one side moves the other,
and the two-view coupling forecast predicts the direction before any
money is spent. A case study
\todo{cot neutralization figure: destructive coupling turned from a
loss increase of $+.38$ into a decrease of $-.40$ by ten anchor
demonstrations} shows that a handful of anchor rows is enough to neutralize the
destructive case, which ties the coupling geometry directly to the
training rule.
