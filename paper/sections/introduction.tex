\section{Introduction}
%% New storyline (v3.0): imitation conflates transfer with distribution
%% shift; teacher as exploration scaffold; verifier-guided
%% self-distillation. House style: no em dashes, no colons, no prose
%% parentheticals, one step of logic per sentence.

Enterprises increasingly deploy language-model agents for narrow,
recurring jobs, such as operating a fixed set of applications,
handling customer requests over database-backed tools, or automating
one workflow \citep{yao2023react, trivedi2024appworld, yao2024taubench,
shridhar2021alfworld}. Serving a frontier model for a single narrow
job is rarely economical, so practice converges on specializing a
small student for that job. What the deployer actually has is a few
dozen example queries from the intended workload, a task-level check
of whether an attempt succeeded, and metered query access to a strong
commercial teacher whose weights and logits are out of reach and whose
every emitted token is billed. The question this paper studies is how
to convert a limited teacher budget into the largest possible gain in
task success for the small student.

The standard answer is imitation. One purchases teacher
demonstrations and trains the student to reproduce them, a recipe that
underlies sequence-level knowledge distillation and its agentic
refinements \citep{kim2016sequence, mukherjee2023orca,
liu2025structured, kang2025distilling}. We find that on a modern agent
benchmark this recipe does not merely underperform, it moves the
student backwards. Across seven current distillation baselines
spanning the labeling, feedback, self-knowledge, and on-policy
branches of the field's taxonomy \citep{xu2024kdsurvey}, every method
that uses teacher tokens as training targets ends below the untrained
student, and the effect is invariant to data selection, student
scale, and data volume. The mechanism is visible in the update
itself. Maximum-likelihood training on teacher trajectories descends
the gradient of the divergence between teacher and student behavior
distributions, so it conflates task-relevant capability transfer with
changes that merely make the student write like the teacher, and for
a small student the second effect dominates. The same training also
erodes abilities the student already had, which we observe directly
as a drop in generic function-calling accuracy on an unrelated
benchmark.

Two further observations point to the way out. The student trained on
its own verified successes loses nothing, and the student shown a
teacher demonstration in its context, rather than as a target, solves
an order of magnitude more tasks than it can solve unaided. Together
they suggest that teacher behavior is valuable as guidance and harmful
as a target. We therefore propose budgeted verifier-guided
self-distillation with teacher context scaffolding. Purchased
demonstrations enter only the student's context, where they raise the
probability that the student discovers successful behavior by itself.
The verifier decides which of the student's own trajectories become
training data, which turns otherwise zero-mean self-generation into a
task-directed learning signal. Guided trajectories are corrected
toward the unguided deployment policy by clipped importance weighting,
and failures contribute only through outcome-contrastive preference
pairs that are masked before the first action divergence and gated by
the learnability of the verified alternative. A gradient-space
representation of task demand decides which demonstrations are worth
buying, an unlock-probability estimate prices each purchase, and
spending stops endogenously once no candidate has positive marginal
utility. As the student improves, queries graduate from guided to
unguided collection, so the procedure approaches ordinary on-policy
self-improvement and the teacher budget is treated as an upper bound
rather than an amount to be spent. After training, a held-out split of
the support set calibrates a conformal task boundary, and the
specialist serves only queries inside it.

Our contributions are as follows. First, we document and analyze the
systematic failure of imitation-based distillation for small agent
students, tracing it to the conflation of capability transfer with
distribution shift and introducing a projection diagnostic that
measures the task-extraneous component of any supervision-induced
update. Second, we develop a complete budgeted specialization
framework in which the teacher is an exploration scaffold, the student
is the sole source of training trajectories, and the verifier is the
sole source of learning signal, with importance-corrected positive
updates, localized negative updates, and endogenous stopping. Third,
across four agent benchmarks under matched budgets, our method is the
only one that improves on its base student while every
imitation-based baseline degrades it, and guided self-generation
raises the student's verified solution rate by an order of magnitude.
\todo{finalize headline numbers at freeze.} Fourth, deployment is
certified, with a split-conformal task boundary that routes
out-of-scope queries away from the specialist.
